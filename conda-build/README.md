This folder contains scripts for building conda packages of the 'ims' executable.

On Linux, run 'cd linux-64 && conda build . -c salford_systems', it will install gcc-5 automatically.

On Mac OS X, gcc-5 must be installed manually through Homebrew.
